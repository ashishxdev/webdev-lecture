- An IIFE is a function that runs immediately after being defined, used to create a private scope and avoid global variables
  (function(name){
  console.log("Hello " + name);
  })("Aashish");
- Destructuring
- Spread Syntax
- Local, Global and Block Scope
- Hoisting
